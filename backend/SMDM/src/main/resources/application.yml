server:
  port: 8080


spring:
  datasource:
      jdbc-url: jdbc:mariadb://localhost:3306/showmedowme_db
      driver-class-name: org.mariadb.jdbc.Driver
      username:
      password:

  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update